{
    "name": "realworld2",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "main": "index.js",
    "scripts": {
        "dev": "turbo run dev",
        "web:dev": "turbo run dev --filter=@repo/web",
        "build": "turbo run build",
        "lint": "turbo run lint",
        "test": "echo \"Error: no test specified\" && exit 1",
        "p:generate": "corepack pnpm --filter @repo/database exec prisma generate",
        "p:migrate": "corepack pnpm --filter @repo/database exec prisma migrate dev",
        "p:validate": "corepack pnpm --filter @repo/database exec prisma validate",
        "p:studio": "corepack pnpm --filter @repo/database exec prisma studio --port 3002",
        "p:push": "corepack pnpm --filter @repo/database exec prisma db push",
        "p:seed": "corepack pnpm --filter @repo/database exec tsx seed.ts",
        "nest:dev": "turbo run dev --filter=@repo/api-nest"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "packageManager": "pnpm@10.32.1",
    "devDependencies": {
        "@playwright/test": "^1.60.0",
        "tsx": "^4.21.0",
        "turbo": "^2.9.3"
    }
}
